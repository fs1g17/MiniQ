info:
  name: Poll Job
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/pollJob
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
